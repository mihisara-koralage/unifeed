[phases.setup]
nixPkgs = ["python313", "python313Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p static && python manage.py collectstatic --noinput"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT config.asgi:application"